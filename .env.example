# AI assistant (Claude) - get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional: enables dense (embedding) retrieval for the RAG chatbot.
# Without it, retrieval falls back to BM25 keyword search - still real RAG.
# Free tier at https://www.voyageai.com
VOYAGE_API_KEY=

# Optional: raises GitHub API rate limits for the activity feed
GITHUB_TOKEN=

# Public site URL used in metadata/sitemap
NEXT_PUBLIC_SITE_URL=https://muthu.dev
